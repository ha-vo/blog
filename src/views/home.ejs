<%- include('partials/header') %>
    <div class="container mt-4">
        <div class="row">
            <% for(let i=0; i < devices.length; i++) { %>
                <div class="col-sm-6 col-lg-4">
                    <div class="card">
                        <img src="<%= devices[i].img %>" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title text-center">
                                <%= devices[i].name %>
                            </h5>
                            <p class="card-text">
                                <%= devices[i].description %>
                            </p>
                            <a href="#" class="btn btn-primary">Xem chi tiết</a> <i class="fa-solid fa-dollar-sign mt-2"
                                style="float:right;">
                                <%= devices[i].price %>
                            </i>
                        </div>
                    </div>
                </div>
                <% } %>
        </div>
    </div>